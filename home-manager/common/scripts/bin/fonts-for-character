#!/usr/bin/env bash

if [ $# -lt 1 ]; then
  echo 1>&2 "$0: not enough arguments"
  exit 2
elif [ $# -gt 1 ]; then
  echo 1>&2 "$0: too many arguments"
  exit 2
fi

printf '%x' \'"$1" | xargs -I{} fc-list ":charset={}"
